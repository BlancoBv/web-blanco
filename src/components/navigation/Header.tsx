import type { FC } from "react"

const Header:FC =()=>{
    return <header className="navbar bg-base-100 sticky top-0 backdrop-blur-sm bg-base-100/80">
        <a className="btn btn-ghost text-xl">daisyUI</a>
    </header>
}
export default Header